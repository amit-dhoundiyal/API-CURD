<!DOCTYPE html>
<html>
  <head>
    <title>Home</title>
    <link rel="shortcut icon" href="/assets/favcon.png" type="image/x-icon">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <% if request.path == root_path || request.path == faq_path || request.path == help_path || request.path == terms_and_conditions_path || request.path == privacy_policy_path %>
    <%= render "layouts/header" %>
      <%end%>
    <%=yield %>
    <% if request.path == root_path || request.path == faq_path || request.path == help_path || request.path == terms_and_conditions_path || request.path == privacy_policy_path  %>
    <%= render "layouts/footer" %>
      <%end%>
  </body>

</html>
